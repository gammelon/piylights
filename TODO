introduce second parameter list that contains min/max values or possible choices for strings
display limits in webinterface
automatic form generation
communication between server and webinterface over tcp
load/store parameters/limits on start/shutdown
save/load presets
authentication for webinterface
restructure { move fx to separate file, rename files according to code ^^)
